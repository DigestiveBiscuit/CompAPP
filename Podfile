# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CompAPP' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CompAPP

  target 'CompAPPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CompAPPUITests' do
    # Pods for testing
  end

pod 'FSCalendar'

end
